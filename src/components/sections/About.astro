---
// About section
// All editable copy lives in src/content/about/about.json

import about from '../../content/about/about.json';
import { Download } from 'lucide-svelte';
---

<section id="about" class="scroll-mt-20 py-section">
  <div class="container mx-auto px-4">
    <h2 class="mb-12 text-3xl font-bold text-foreground md:text-4xl">{about.sectionTitle}</h2>

    <div class="grid gap-12 md:grid-cols-2 md:gap-16">
      <!-- Photo Column -->
      <div class="flex flex-col items-center md:items-start">
        <div
          class="relative mb-6 h-64 w-64 overflow-hidden rounded-lg border-2 border-border bg-card shadow-lg md:h-80 md:w-80"
        >
          <!-- Placeholder for professional photo -->
          <div
            class="flex h-full w-full items-center justify-center bg-gradient-to-br from-primary/20 to-primary/5 text-4xl font-bold text-primary"
          >
            AE
          </div>
        </div>

        <!-- Resume Download Button -->
        <a
          href={about.resume.path}
          download
          class="inline-flex items-center gap-2 rounded-lg bg-primary px-6 py-3 font-medium text-white transition-colors hover:bg-primary/90"
        >
          <Download class="h-5 w-5" aria-hidden="true" />
          {about.resume.buttonLabel}
        </a>
      </div>

      <!-- Content Column -->
      <div class="space-y-6">
        <div>
          <h3 class="mb-4 text-2xl font-semibold text-foreground">
            {about.subheading}
          </h3>
          {
            about.paragraphs.map((paragraph) => (
              <p class="mb-4 text-lg leading-relaxed text-secondary">{paragraph}</p>
            ))
          }
        </div>

        <!-- Key Achievements -->
        <div>
          <h4 class="mb-3 text-xl font-semibold text-foreground">Key Achievements</h4>
          <ul class="space-y-2 text-secondary">
            {
              about.achievements.map((item) => (
                <li class="flex items-start gap-2">
                  <span class="mt-1 text-primary">•</span>
                  <span>{item}</span>
                </li>
              ))
            }
          </ul>
        </div>

        <!-- Education -->
        <div>
          <h4 class="mb-3 text-xl font-semibold text-foreground">Education</h4>
          <div class="text-secondary">
            <p class="font-semibold text-foreground">{about.education.school}</p>
            <p class="mt-1">{about.education.degree}</p>
            <p class="mt-1 text-sm">{about.education.note}</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
